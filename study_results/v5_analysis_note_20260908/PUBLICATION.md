# v5.0.0 repository publication

This publication preserves the 138-page reviewed PDF and all 408 entries in MANIFEST.json unchanged. It adds the editable source archive, final rendered-page QA, output copies, and six hash-verified source files needed by the v5 figures that were not already on main.

The publication branch starts at caa50d543. The original authoring base 0c1d4692b contains a much broader local snapshot and is deliberately not merged. HANDOFF.md and provenance/session_delivery.json record the earlier authoring session; their statements that no commit or push had occurred are historical.

The LaTeX document rebuilds from the bundled source and figures. Figure regeneration also requires the archived repository studies and the three external released ROOT inputs recorded in the figure ledger. No new data, fits or toys were introduced by publication.

See provenance/publication.json for the exact added input files and original PDF hash.
