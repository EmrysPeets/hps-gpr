# Continuation from v5.0.1

This revision changes explanation and presentation only. Its baseline is the published v5.0.0 merge `3c557e391`. The original authoring checkout and all frozen studies were preserved.

Before changing any scientific result, identify its frozen source in `provenance/v500_source_claims.json` and `provenance/v500_figure_provenance.json`. The v5.0.1 figure and historical ledgers identify all additional displays. Use the current PDF and request checklist as the manuscript entry points.

The unresolved statistical-tail continuation is retained in `derived/unresolved_tail_continuation.csv` and the inherited source studies. The note does not establish final particle-global probabilities or qualify the remaining 2021 data for unblinding. Those questions require a separately scoped study, suitable background controls and the declared full-scan treatment.

The current-versus-full-2021 comparison applies the historical density prescription to the saved observed curve: only the 2021 normalization density is multiplied by ten. Do not relabel that curve as expected sensitivity.

For later edits, rebuild the PDF, repeat rendered-page and text QA, update manifests, and check a portable rebuild before publishing. Source archive checksums bind this delivered revision; do not silently replace its files with a later build.
