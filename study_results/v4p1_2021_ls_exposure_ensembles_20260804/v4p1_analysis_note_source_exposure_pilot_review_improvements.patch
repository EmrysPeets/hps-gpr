diff --git a/hps_gpr_analysis_note/FIGURE_MANIFEST.md b/hps_gpr_analysis_note/FIGURE_MANIFEST.md
index 519283b..9258fc6 100644
--- a/hps_gpr_analysis_note/FIGURE_MANIFEST.md
+++ b/hps_gpr_analysis_note/FIGURE_MANIFEST.md
@@ -122,8 +122,9 @@ section now uses the following copied limit, significance, and projection assets
 | Bundle path | Original source | Status | Notes |
 | --- | --- | --- | --- |
 | `toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ls_observed_dataset_comparison.pdf` | `study_results/v4p1_2021_ls_exposure_ensembles_20260804/plots/fig_v4p1_ls_observed_dataset_comparison.pdf` | fit-space diagnostic | Exact observed 2016 10%-versus-full and 2021 1%-versus-10% optimized-length-scale comparison. No toy-derived quantity or expected band is shown. |
-| `toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ls_toy_ensemble_curves.pdf` | `study_results/v4p1_2021_ls_exposure_ensembles_20260804/plots/fig_v4p1_ls_toy_ensemble_curves.pdf` | pilot diagnostic | Ten individual refitted hyperparameter curves for each of the five declared native/scaled scenarios. The native-10% versus `1%-truth x10` comparison is a source/selection-shape diagnostic because their support totals differ. |
-| `toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ls_bound_sensitivity.pdf` | `study_results/v4p1_2021_ls_exposure_ensembles_20260804/plots/fig_v4p1_ls_bound_sensitivity.pdf` | pilot diagnostic | Ten-toy upper-bound, nested-LML, and optimizer-stability screen across factors 6, 9, 12, 15, 20, and 25. It is not a physics-card freeze. |
+| `toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ensemble_ls_all_toys_gengamma.pdf` | `study_results/v4p1_2021_ls_exposure_ensembles_20260804/plots/fig_v4p1_ensemble_ls_all_toys_gengamma.pdf` | pilot diagnostic | Multipage primary-lane export of the ten individual refitted length-scale trajectories for each of the five declared native/scaled scenarios and each tested upper factor. The native-10% versus `1%-truth x10` comparison is a source/selection-shape diagnostic because their support totals differ. |
+| `toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ensemble_bound_lml_diagnostics_gengamma.pdf` | `study_results/v4p1_2021_ls_exposure_ensembles_20260804/plots/fig_v4p1_ensemble_bound_lml_diagnostics_gengamma.pdf` | pilot diagnostic | Primary-lane ten-toy upper-bound occupancy and nested-LML screen across factors 6, 9, 12, 15, 20, and 25 after unchanged-card optimizer review. It is not a physics-card freeze. |
+| `toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ensemble_fixed_amplitude_response_gengamma.pdf` | `study_results/v4p1_2021_ls_exposure_ensembles_20260804/plots/fig_v4p1_ensemble_fixed_amplitude_response_gengamma.pdf` | pilot diagnostic | Primary-lane fixed-amplitude response screen. Absolute injected amplitudes are calibrated once on the factor-15 reference and reused at every upper factor, so changes in extraction uncertainty, response, residual, or pull are not caused by changing the injected target. Ten toys do not establish bias, coverage, or sensitivity qualification. |
 
 No v4.1 expected-limit-band or upper-limit-tail-ensemble asset exists. The ten-toy
 source-and-exposure assets above are fit-space pilot diagnostics and must not be
diff --git a/hps_gpr_analysis_note/hps_gpr_analysis_note.bib b/hps_gpr_analysis_note/hps_gpr_analysis_note.bib
index 68e4b79..ae05b79 100644
--- a/hps_gpr_analysis_note/hps_gpr_analysis_note.bib
+++ b/hps_gpr_analysis_note/hps_gpr_analysis_note.bib
@@ -159,6 +159,21 @@
   url = {https://arxiv.org/abs/1709.05681}
 }
 
+@article{Dauncey2015DiscreteProfiling,
+  author = {Dauncey, P. D. and Kenzie, M. and Wardle, N. and Davies, G. J.},
+  title = {Handling Uncertainties in Background Shapes: The Discrete Profiling Method},
+  journal = {JINST},
+  volume = {10},
+  number = {04},
+  pages = {P04015},
+  year = {2015},
+  doi = {10.1088/1748-0221/10/04/P04015},
+  eprint = {1408.6865},
+  archivePrefix = {arXiv},
+  primaryClass = {physics.data-an},
+  url = {https://arxiv.org/abs/1408.6865}
+}
+
 @article{ATLASSpuriousSignal2024,
   author = {{ATLAS Collaboration}},
   title = {Search for Low-Mass Resonances Decaying into Two Jets and Produced in Association with a Photon or a Jet at {$\sqrt{s}=13$ TeV} with the {ATLAS} Detector},
diff --git a/hps_gpr_analysis_note/sections/04_methodology.tex b/hps_gpr_analysis_note/sections/04_methodology.tex
index 1eafc99..11d9c52 100644
--- a/hps_gpr_analysis_note/sections/04_methodology.tex
+++ b/hps_gpr_analysis_note/sections/04_methodology.tex
@@ -231,16 +231,41 @@ and uses this local physical scale to set the RBF bounds. In the
 \ell_{\max,d}(m) = k_{\max,d}\sigma_x(m).
 \label{eq:dataset-lengthscale-bounds}
 \end{equation}
-For the RBF covariance, $\ell$ is the characteristic separation in the GP input
-coordinate over which the prior covariance decays. It controls the scale of
-variations admitted by the background model
-\cite{RasmussenWilliams2006}; without an additional mapping, it is not a measured
-detector or background-physics correlation length. Likewise, the lower and upper
-values in Eq.~\eqref{eq:dataset-lengthscale-bounds} define the numerical optimizer
-domain. An optimum at $\ell_{\max}$ establishes that the constraint is active for
-that fit, not that the physical background correlation scale has been measured at
-the boundary. The implementation optimizes log-transformed kernel
-hyperparameters within these configured bounds \cite{ScikitLearnGPRDocs16}.
+For the RBF covariance used here,
+\begin{equation}
+k_{\mathrm{RBF}}(x,x') =
+\sigma_f^2\exp\!\left[-\frac{(x-x')^2}{2\ell^2}\right],
+\qquad
+\frac{k_{\mathrm{RBF}}(x,x+\ell)}{k_{\mathrm{RBF}}(x,x)}
+=e^{-1/2}\simeq0.61 .
+\label{eq:rbf-lengthscale-interpretation}
+\end{equation}
+Thus $\ell$ is a covariance-decay scale in the GP input coordinate: larger values
+favor smoother background functions, while smaller values admit more rapid
+variation \cite{RasmussenWilliams2006,FrateGP2017}. The ratio
+$\ell/\sigma_x(m)$ expresses that nuisance-model smoothness relative to the width of
+the narrow signal hypothesis. It is not, without an independently validated mapping,
+a detector resolution measurement or a physical background correlation length.
+The fitted $\ell_{\mathrm{opt}}$ is the log-marginal-likelihood optimum conditional
+on the kernel, data support, sideband mask, noise model, and configured optimizer
+domain. Consequently an optimum at $\ell_{\max}$ establishes only that the numerical
+constraint is active; it neither measures a physical scale at the boundary nor
+locates the unconstrained optimum. The implementation optimizes log-transformed
+kernel hyperparameters within these configured bounds
+\cite{ScikitLearnGPRDocs16}.
+
+This distinction determines how the hyperparameter study enters a physics result.
+If the allowed range forces $\ell$ too low, the GP retains unnecessary
+signal-scale flexibility and can absorb part of a localized excess; if the fitted
+background is forced too smooth, broad mismodeling can instead leave a biased local
+residual. Raising only the upper bound permits, but does not require, a smoother
+solution. A larger data set also need not prefer a larger $\ell$: additional
+statistics can resolve background structure and move the optimum downward. The
+upper range is therefore selected from boundary occupancy, likelihood ordering,
+optimizer stability, and fixed-amplitude signal-recovery diagnostics, never from a
+favorable observed limit or local $p_0$. Only after those procedural checks and
+coverage qualification may the range be treated as part of a physics analysis
+card.
 
 The v4.1 observed/asymptotic candidate uses the dataset-specific factors in
 Table~\ref{tab:production-lengthscale-bounds}. The lower factors retain their earlier
@@ -330,7 +355,9 @@ source/selection-shape diagnostic, including the different native normalization,
 than a pure tenfold-exposure closure test. The primary lane uses the same
 \texttt{fGenGammaThresh} analytic family for both native sources; the independently
 declared \texttt{fSigPowExpQ} lane is an alternate smooth-truth stress test. Neither
-family is asserted to be the physical trident generator.
+family is asserted to be the physical trident generator. Testing a second smooth
+family probes background-shape dependence, but it does not implement the
+discrete-profiled function-choice nuisance of Ref.~\cite{Dauncey2015DiscreteProfiling}.
 
 Every pseudo-spectrum is passed through a fresh GP sideband fit with the production
 40--300~MeV support, 50--250~MeV search interval, rebin-five geometry, and
@@ -342,6 +369,25 @@ occupancy, log marginal likelihood, optimizer status, count-space amplitude
 diagnostics, and repeat stability. Questionable optimizer branches must be rerun with
 the toy and card unchanged; they must not be repaired by interpolation.
 
+The signal-response screen uses fixed absolute amplitudes at the predeclared
+\SIlist{60;120;220}{MeV} injection masses. For each truth, scenario, toy, and mass,
+the factor-15 background-only prefit-Asimov calibration defines
+$\sigma_{A,\mathrm{ref}}^{(15)}$, and the predeclared levels
+$z\in\{0,1,3,5\}$ define
+\begin{equation}
+A_{\mathrm{inj}}(z)=z\,\sigma_{A,\mathrm{ref}}^{(15)} .
+\label{eq:v4p1-fixed-amplitude-anchor}
+\end{equation}
+The same numerical $A_{\mathrm{inj}}$ is then reused at every tested upper factor;
+otherwise an apparent factor dependence could be produced simply by moving the
+injection target. This is an Asimov calibration of the local response axis in the
+sense of Ref.~\cite{Cowan2011}, not an expected-limit construction. At fixed
+$A_{\mathrm{inj}}$, an increase in $\sigma_A$ indicates degraded local extraction
+sensitivity, a displacement of $\hat A-A_{\mathrm{inj}}$ diagnoses signed bias, and
+a response $(\hat A_z-\hat A_0)/A_{\mathrm{inj}}$ below unity diagnoses signal
+absorption. Pull widths test the associated uncertainty calibration. With ten toys
+these quantities are implementation screens, not precise ensemble statements.
+
 This is a screening pilot in fit space. The immutable fit path used for the scan
 predates the v4 uncropped fractional-bin density conversion, so its exported
 $\eps^2$ values are explicitly non-promotable. The pilot produces no expected-limit
diff --git a/hps_gpr_analysis_note/sections/05_toys_validation.tex b/hps_gpr_analysis_note/sections/05_toys_validation.tex
index 828037d..de97d05 100644
--- a/hps_gpr_analysis_note/sections/05_toys_validation.tex
+++ b/hps_gpr_analysis_note/sections/05_toys_validation.tex
@@ -539,9 +539,10 @@ monotonic luminosity dependence.}
 
 \begin{figure}[H]
 \centering
-\graphicorplaceholder{0.94\linewidth}{toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ls_toy_ensemble_curves.pdf}
+\graphicorplaceholder{0.94\linewidth}{toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ensemble_ls_all_toys_gengamma.pdf}
 \caption{Individual optimized-length-scale curves in the ten-toy 2021
-source-and-exposure pilot. Curves are shown separately for native 1\%,
+source-and-exposure pilot primary lane. Separate pages show each tested upper factor;
+within a page, curves are shown separately for native 1\%,
 $1\%\times10$, $1\%\times100$, native 10\%, and $10\%\times10$. They are
 projections of a fit-space hyperparameter diagnostic under the declared smooth truth,
 not projected limits, expected-limit bands, or a prediction of the eventual full-2021
@@ -553,23 +554,42 @@ source/selection shape and native normalization.}
 
 \begin{figure}[H]
 \centering
-\graphicorplaceholder{0.92\linewidth}{toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ls_bound_sensitivity.pdf}
-\caption{Upper-bound sensitivity in the same ten-toy pilot for
-$k_{\max}=6,9,12,15,20,$ and 25. The panels summarize boundary occupancy,
-log-marginal-likelihood ordering, and optimizer-repeat stability on the predeclared
-mass grid. A bound is not selected because it gives a favorable limit or local
-$p_0$. With only ten toys per scenario, even zero observed boundary hits is a screening
-diagnostic rather than a production qualification.}
+\graphicorplaceholder{0.92\linewidth}{toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ensemble_bound_lml_diagnostics_gengamma.pdf}
+\caption{Upper-bound occupancy and likelihood diagnostic in the same ten-toy pilot for
+$k_{\max}=6,9,12,15,20,$ and 25. The panels summarize row- and toy-level boundary
+occupancy and the median and worst adjacent-factor log-marginal-likelihood changes
+on the predeclared mass grid after unchanged-card optimizer review. A bound is not
+selected because it gives a favorable limit or local $p_0$. With only ten toys per
+scenario, even zero observed boundary hits is a screening diagnostic rather than a
+production qualification.}
 \label{fig:v4p1-ls-bound-sensitivity}
 \end{figure}
 
+\begin{figure}[H]
+\centering
+\graphicorplaceholder{0.94\linewidth}{toy_generation_figs/v4p1_ls_exposure_pilot/fig_v4p1_ensemble_fixed_amplitude_response_gengamma.pdf}
+\caption{Fixed-amplitude signal-response screen for the primary truth lane. At each
+truth, scenario, toy, and injection mass, the absolute injected amplitudes are
+calibrated once from the factor-15 background-only Asimov reference according to
+Eq.~\eqref{eq:v4p1-fixed-amplitude-anchor} and then reused unchanged at every upper
+factor. The panels compare extraction uncertainty, signed residual, paired recovery,
+pull width, and injection-refit boundary occupancy. An increased $\sigma_A$ is the
+defined indicator of reduced local extraction sensitivity; a nonzero residual or
+response away from unity is the corresponding bias or absorption diagnostic. With
+ten toys, the figure is an implementation screen and neither a sensitivity
+qualification, a coverage result, nor an expected-limit band.}
+\label{fig:v4p1-ls-fixed-amplitude-response}
+\end{figure}
+
 \paragraph{Numerical pilot summary.}
 \VFourPOneLSPilotNumericalSummary
 
 The production replacement for the preceding placeholder should report, for each truth,
 scenario, and upper factor, the completed-fit fraction, upper-bound occupancy, median
 and range of $\ell_{\mathrm{opt}}/\sigma_x$, adjacent-factor
-log-marginal-likelihood ordering, and unchanged-card repeat stability. These ten-toy
+log-marginal-likelihood ordering, unchanged-card repeat stability, and the
+factor-15-anchored $\sigma_A$ ratio, signed residual, paired recovery, and pull-width
+diagnostics. These ten-toy
 summaries must remain labeled as pilot diagnostics. They do not retroactively validate
 the 2016 factor-12 observed candidate, select a final 2021 upper range, qualify the
 non-promotable $\eps^2$ export, or supply coverage, expected bands, or a global
diff --git a/hps_gpr_analysis_note/sections/07_conclusions.tex b/hps_gpr_analysis_note/sections/07_conclusions.tex
index 3c25953..8226c2b 100644
--- a/hps_gpr_analysis_note/sections/07_conclusions.tex
+++ b/hps_gpr_analysis_note/sections/07_conclusions.tex
@@ -36,10 +36,13 @@ production-geometry GP refit, and hyperparameter export for five declared scenar
 native 1\%, $1\%\times10$, $1\%\times100$, native 10\%, and $10\%\times10$.
 The native-10\% support total is 11.296 times the native-1\% total, so the
 $1\%\times10$ versus native-10\% comparison probes source/selection shape and native
-normalization rather than a pure exposure scaling. The individual curves and
-upper-bound summaries are pilot fit-space diagnostics. Ten toys are too few to
-establish stable tail probabilities or coverage, and no expected-limit band is
-constructed from this screen.
+normalization rather than a pure exposure scaling. The individual curves,
+upper-bound summaries, and factor-15-anchored fixed-amplitude response are pilot
+fit-space diagnostics. Holding the absolute injected amplitude fixed across bounds
+makes changes in $\sigma_A$, residual, response, and pull interpretable as changes
+in the extraction rather than changes in the target signal. Ten toys are too few to
+establish stable bias, sensitivity, tail-probability, or coverage statements, and no
+expected-limit band is constructed from this screen.
 
 The production follow-up should use a predeclared larger ensemble, retain paired toy
 identifiers within each truth-source scaling family and across upper-bound settings,
