# Stale toy-build summary

`rigid_toy_build_summary.json` is retained only as development provenance.  It
records manifest SHA-256 `501c61c4...`, whereas the frozen current manifest has
SHA-256 `3d8f981d...`.  The ROOT payload itself is unchanged and retains
SHA-256 `216f5007...`.

Use `FINAL_ARTIFACT_SHA256.txt`, `rigid_study_spec.json`, and the current input
manifest as the authoritative release records.  Do not use this summary to
validate or select an input product.
