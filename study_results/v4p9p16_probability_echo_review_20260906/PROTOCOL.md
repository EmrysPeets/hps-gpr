# Figure 1 probability audit and signal-echo interpretation

This derivative leaves the sealed combined, deficit and extraction studies unchanged. It updates the presentation of their probabilities and adds an explicitly conditional signal-echo check. No upper limit, observed fitted amplitude, mass selection, production kernel, binning used in inference or existing global-test ordering is changed.

## Probability audit and display rules

Reconstruct all 232 profiled signed roots, reference offsets, response widths, local tails and both global orderings from the saved observed scan, full joint toy vectors and GP maxima. Compare the selected extraction roots with the corresponding dense combined/individual references. Count direct local and global exceedances using the same complete 1,000 joint spectra. Preserve the raw-positive gate of the existing stress-centered test: nonpositive observed roots have p=1. The nominal asymptotic discovery convention instead has p0=0.5 for nonpositive roots. Record both definitions; do not smooth away their real discontinuities or replace a probability to make the display look nicer.

Figure 1 leads with the unchanged observed upper limit, signed fitted root and nominal asymptotic local p0. Move the separate background-referenced local/global diagnostics to a dedicated figure and explain their null construction in both captions. Remove the old v4.9.12 comparison overlay/legend and the archived-stress subtitle from Figure 1. Keep the model dependence and finite-toy limitations explicit in the caption and text.

Show every hypothesis. Zero exceedances are one-sided 95% Monte Carlo upper bounds, never zero probabilities. Mark estimates with fewer than 25 exceedances as sparse; do not connect them as a precision curve. Plot analytic local tails only down to a declared 1e-4 display floor, with downward symbols for smaller formal values. Those formal Gaussian continuations are unvalidated far-tail extrapolations, not measured discovery probabilities. Retain their exact values in a comparison table for auditing. Global GP sample counts quantify approximation sampling only, while the 1,000 direct scans have coarser resolution. Moving the pre-existing tests between figures changes presentation, not inference.

## Signal echoes

First verify and reuse the earlier 2021 peak-dip, reverse-injection and double-injection ledgers. Re-evaluate deterministic background and positive-only injections with the current exact dense profiled solver on the same saved smooth truth and full native spectrum. Use masses 60--88 MeV and the original fixed per-mass kernel states. Evaluate background, 66 MeV, 78 MeV, and simultaneous 65+78 MeV injections; record the two standalone injected yields rather than re-estimating them to improve agreement. The archived amplitudes and generating truth were selected from the old data, so this is a mechanism check, not a probability calculation or a fit selecting between one and two particles.

Subtract the same deterministic background-only root at each mass to isolate the injected change. Compare the current observed 2021 roots and inspect the background displacement at 71/72 MeV. Reuse the original paired ten background and ten signal-plus-background pilot spectra if needed for context; generate no new random toys. Keep inter-mass null correlations, injected response and physical origin as three different questions. A signal or fluctuation can enter another hypothesis's training sidebands and produce positive and negative fitted echoes. A covariance matrix alone cannot identify which observed peak is physical.

## Report and validation

Revise the extraction discussion so a neighboring fitted deficit is not automatically evidence against an underlying positive resonance. Explain why growth with exposure can also occur in an echo. Retain the staged 10%, additional20%,30%,100% interpretation and the completed 2015 low-mass side section. Require independent HEP/statistical audit, numerical artifact checks, source hashes, semantic PDF checks and rendered-page review. Store all changes in this derivative and its own output directory; preserve the shared index and checkout.
