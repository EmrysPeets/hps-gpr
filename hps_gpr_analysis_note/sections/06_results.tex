\section{Results}
\label{sec:results}

This section summarizes the corrected limit and projection products used for the
current internal review. The plots shown here are copied from the corrected CSV-driven
summaries so that the note, Overleaf upload, and local compilation use the same figure
inputs. The section is organized as a short guide:
\begin{itemize}
\item What are the individual dataset inputs?
\item What is the corrected simultaneous shared-$\eps^2$ limit?
\item Which observed fluctuations affect the limit and discovery diagnostics?
\item What full-statistics reach is projected?
\item How does that projection compare with published HPS and BaBar exclusions?
\end{itemize}

The central result remains a simultaneous shared-$\eps^2$ analysis. At a fixed mass
hypothesis a single coupling value predicts a dataset-dependent signal yield through
the conversion factors of Sections~\ref{sec:methodology} and
\ref{sec:event-selection}. The individual 2015, 2016 10\%, and 2021 1\% limits are
therefore useful diagnostics and comparison curves, but the combined curve is not made
by taking the envelope of those individual upper limits. It is the direct result of the
profiled simultaneous likelihood in the common coupling parameter.

\subsection{Individual data set results}
\label{sec:results-individual-inputs}

Figure~\ref{fig:results-individual-eps2} shows the corrected individual-dataset
$\eps^2$ limit summaries for the three inputs that enter the current combination. The
2015 curve covers the lower mass range, the 2016 10\% curve extends into the middle
and upper prompt-search region, and the 2021 1\% curve provides the widest high-mass
coverage. These panels are included for two reasons: they provide a direct check that
the yield-to-coupling conversion is sensible dataset by dataset, and they make clear
which single-dataset fluctuations can influence the simultaneous observed limit.

\begin{figure}[h!]
\centering
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/individual_datasets/2015_eps2_coupling_bands_observed_expected.png}
  \caption{2015.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/individual_datasets/2016_eps2_coupling_bands_observed_expected.png}
  \caption{2016 10\%.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/individual_datasets/2021_eps2_coupling_bands_observed_expected.png}
  \caption{2021 1\%.}
\end{subfigure}
\caption{Individual 95\% CL upper limits on $\eps^2$ for the three datasets used in
the corrected simultaneous combination. The observed curves are shown together with
their expected bands. These figures are diagnostic inputs to the shared-coupling
combination; the combined result is obtained from the joint likelihood, not by
selecting the lowest individual curve.}
\label{fig:results-individual-eps2}
\end{figure}

The corresponding local discovery $p_0$ scans are shown in
Figure~\ref{fig:results-individual-p0}. These curves answer a different question than
the upper limits: they test whether the observed spectrum contains a positive local
excess at a fixed mass. They are included to identify the mass regions where an
observed upper limit may weaken because a single dataset fluctuates upward. The global
overlays are effective-trials references and should not be treated as the final
discovery calibration.

\begin{figure}[h!]
\centering
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/individual_datasets/2015_local_p0_with_global_p_overlay.png}
  \caption{2015.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/individual_datasets/2016_local_p0_with_global_p_overlay.png}
  \caption{2016 10\%.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/individual_datasets/2021_local_p0_with_global_p_overlay.png}
  \caption{2021 1\%.}
\end{subfigure}
\caption{Individual one-sided local $p_0$ scans. These plots locate the local upward
fluctuations in the input spectra. They are diagnostic inputs to the combined
significance discussion; the final global discovery statement should be based on the
maximum combined discovery statistic over the scan range.}
\label{fig:results-individual-p0}
\end{figure}

\subsection{Corrected simultaneous observed limit}
\label{sec:results-combined-151621-1pct}

The corrected observed and expected simultaneous limit is shown in
Figure~\ref{fig:results-combined-corrected-band}. The expected bands are taken from
the corrected combined-band summary, while the observed curve is the corrected
CSV-merged observed limit. This separation is useful because the expected bands are
already stable, whereas the observed curve required targeted checks of several
mass-point exports. The resulting presentation keeps the full corrected band envelope
and overlays the corrected observed limit on the same $\eps^2$ axis.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.90\linewidth}{final_limit_projection_figs/combined/combined_corrected2_bands_with_correct_observed_eps2.png}
\caption{Corrected simultaneous 2015$+$2016 10\%$+$2021 1\% upper limit on
$\eps^2$. The green and yellow bands are the corrected expected 68\% and 95\%
background-only intervals, and the black curve is the corrected observed
shared-coupling limit. This is the primary current band figure for the simultaneous
analysis.}
\label{fig:results-combined-corrected-band}
\end{figure}

Figure~\ref{fig:results-observed-overlay} overlays the corrected combined observed
limit with the three individual observed limits. This comparison is deliberately not a
combination prescription. It is a diagnostic showing whether the shared fit behaves in
a physically recognizable way relative to the constituent datasets. In the genuine
overlap region, a downward background fluctuation or a stronger expected constraint in
one dataset can pull the best individual curve below the simultaneous observed curve.
Conversely, when the datasets have compatible behavior, the simultaneous test can give
a stronger observed limit than any single input.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.90\linewidth}{final_limit_projection_figs/combined/observed_eps2_overlay_individual_and_correct_combined.png}
\caption{Observed 95\% CL upper limits on $\eps^2$ for the individual datasets and the
corrected simultaneous combination. The 2016 input is the 10\% subset and the 2021
input is the 1\% subset. The combined curve is the direct simultaneous result in a
shared coupling parameter, not the pointwise minimum of the individual curves.}
\label{fig:results-observed-overlay}
\end{figure}

The observed-data extraction display in
Figure~\ref{fig:results-combined-151621-1pct-display} shows a representative mass point
from the same corrected simultaneous workflow. It is included here, before the
projection material, because it exposes the dataset-level spectra, profiled backgrounds,
and extracted signal components that underlie the shared-coupling limit curve.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.96\linewidth}{observed_display_combined_119MeV.png}
\caption{Observed shared-coupling extraction display at a mass hypothesis of
\SI{119}{MeV}. The top row shows, for 2015, 2016, and 2021 separately, the observed
mass spectrum in the fit region together with the GP background prediction, the
profiled background, the best-fit signal$+$background model, and the blinded window.
The middle row recasts the same mass window as residuals relative to the profiled
background, overlays the extracted signal component for each dataset, and includes the
residual uncertainty bands used throughout the observed-data validation workflow. The
lower panel maps the extracted signal templates into the common variable
$(m-m_0)/\sigma_m$ and shows how the dataset-level extractions sum to the combined
black curve.}
\label{fig:results-combined-151621-1pct-display}
\end{figure}

The ratio in Figure~\ref{fig:results-combined-best-ratio} makes the same diagnostic
more quantitative. It plots the observed combined upper limit in $\eps$ divided by the
best, meaning lowest, active individual observed upper limit in $\eps$ at each mass.
Values below one indicate that the simultaneous observed result improves on every
active single-dataset observed limit. Values above one indicate that, at that mass, the
best individual dataset alone has a stronger observed exclusion than the simultaneous
fit. This can happen for observed limits because the combined test must accommodate the
data in all active spectra at one common coupling; it is not evidence by itself that
the expected simultaneous sensitivity is worse. It is instead a useful audit of where
observed upward fluctuations, active-dataset changes, or conversion-factor differences
drive the shape of the combined observed curve.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.90\linewidth}{final_limit_projection_figs/diagnostics/combined_vs_best_individual_observed_epsilon_ratio.png}
\caption{Diagnostic ratio of the corrected observed simultaneous upper limit in
$\eps$ to the best active individual observed upper limit in $\eps$. A ratio below one
means the simultaneous observed limit is stronger than each individual observed limit;
a ratio above one means the best individual dataset gives the tighter observed limit at
that mass. The ratio is not an expected-sensitivity metric; it is an observed-data
audit of how the shared-coupling fit responds to the active datasets.}
\label{fig:results-combined-best-ratio}
\end{figure}

\subsection{Limit-tail and discovery diagnostics}
\label{sec:results-discovery-diagnostics}

The limit-tail diagnostics and the discovery $p_0$ scan are intentionally kept
separate. Figure~\ref{fig:results-limit-tail-diagnostic} compares the observed CL$_s$
upper limit with the background-only limit ensemble and shows the corrected local and
global reference thresholds. These quantities test how unusually strong or weak the
observed limit is. They are not the discovery p-value.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.88\linewidth}{final_limit_projection_figs/diagnostics/combined_toy_limit_tail_areas_with_corrected_global_thresholds.png}
\caption{Toy-limit diagnostic for the simultaneous upper limit, with corrected global
reference thresholds. The dashed global-threshold lines use the inverse trials
correction, so the local p-value required for a fixed global significance lies below
the corresponding local-only reference. This panel diagnoses the observed limit
relative to background-only limit toys; it is distinct from the discovery statistic.}
\label{fig:results-limit-tail-diagnostic}
\end{figure}

The discovery-oriented local/global curves are shown in
Figure~\ref{fig:results-combined-significance}. The local $p_0$ curve is the
one-sided fixed-mass profile-likelihood discovery test. The global overlay is the
effective-trials approximation to the scan hypertest and should be replaced by a
toy-calibrated $q_{0,\max}$ distribution for any final discovery claim.

\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/diagnostics/combined_local_p0_with_global_p_overlay.png}
  \caption{Local and approximate global $p_0$.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/diagnostics/combined_local_Z_with_global_Z_overlay.png}
  \caption{Equivalent local and approximate global $Z$.}
\end{subfigure}
\caption{Discovery-oriented local/global diagnostics for the corrected simultaneous
analysis. The local curve is the fixed-mass one-sided discovery test. The global curve
is an effective-trials approximation and is shown to guide review; the final
look-elsewhere result should come from a simultaneous scan toy calibration.}
\label{fig:results-combined-significance}
\end{figure}

\subsection{Full-statistics projections and published HPS comparison}
\label{sec:results-full-projections}

The projected full-data reach is an observed-equivalent scaling study. The 2015 sample
is held fixed, the 2016 10\% subset is scaled to 100\%, and the 2021 1\% subset is
scaled to 100\%. The scaling is applied through the local density/exposure factor used
in the yield-to-$\eps^2$ conversion, so the projected $\eps^2$ limit scales as
$\sqrt{\rho_{\mathrm{current}}/\rho_{\mathrm{projected}}}$ at each mass before the
comparison curves are drawn. This is a sensitivity projection, not a substitute for a
new observed full-statistics simultaneous fit.

Figure~\ref{fig:results-projection-published-combined} compares the projected
2015$+$2016$+$2021 full-data simultaneous reach with the published 2016 HPS upper
limit. The lower ratio panel shows that the full-data simultaneous projection is
substantially stronger than the published 2016 result over most of the plotted range;
the median projected/published ratio is about 0.11, corresponding to a median
improvement factor of about 9.2 in $\eps^2$.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.92\linewidth}{final_limit_projection_figs/projections/published_2016_vs_projected_combined_100pct_eps2.png}
\caption{Projected 2015$+$2016$+$2021 100\% simultaneous $\eps^2$ reach compared with
the published 2016 HPS upper limit. The ratio panel shows the projected limit divided
by the published limit, so values below one indicate an improvement relative to the
published 2016 result.}
\label{fig:results-projection-published-combined}
\end{figure}

Figure~\ref{fig:results-projection-published-2016} isolates the 2016 scaling by
promoting the current 2016 10\% observed-equivalent curve to 2016 100\%. This is a
cleaner check of exposure scaling because it does not mix detector configurations or
mass ranges. The median projected/published ratio is about 0.71, so the 2016-only
100\% projection improves on the published curve on average but not uniformly at every
mass.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.92\linewidth}{final_limit_projection_figs/projections/published_2016_vs_projected_2016_100pct_eps2.png}
\caption{Projected 2016 100\% observed-equivalent $\eps^2$ reach compared with the
published 2016 HPS upper limit. This plot isolates the effect of scaling the 2016
10\% development sample to the full 2016 exposure.}
\label{fig:results-projection-published-2016}
\end{figure}

For a compact visual summary, Figure~\ref{fig:results-projection-overlay} overlays the
published 2016 result, the 2016-only 100\% projection, and the full simultaneous
100\% projection. The full simultaneous projection is much deeper because the 2021
sample is promoted from 1\% to 100\% and because the shared-coupling analysis can pool
independent datasets where their mass ranges overlap.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.88\linewidth}{final_limit_projection_figs/projections/published_2016_vs_100pct_projection_overlay_eps2.png}
\caption{Overlay of the published 2016 HPS upper limit, the projected 2016 100\%
observed-equivalent reach, and the projected 2015$+$2016$+$2021 100\% simultaneous
reach.}
\label{fig:results-projection-overlay}
\end{figure}

\subsection{BaBar comparison}
\label{sec:results-babar-comparison}

The observed 90\% upper-limit contour used in
Figure~\ref{fig:results-babar-projection} is the contour provided by the BaBar
collaboration for the visible-dark-photon search. For a like-for-like visual comparison
to the HPS 95\% CL curves, the plotted BaBar curve is converted from a 90\% limit on
$\eps$ to an approximate 95\% limit on $\eps^2$ with the one-sided Gaussian factor
\begin{equation}
\eps^2_{95}=\eps^2_{90}\frac{1.64485362695}{1.28155156554}
=1.28348610\,\eps^2_{90}.
\end{equation}
This is an approximate confidence-level conversion, but it is the appropriate
normalization for a faithful 95\%-versus-95\% visual comparison in the absence of the
original BaBar likelihood.

\begin{figure}[H]
\centering
\graphicorplaceholder{0.92\linewidth}{final_limit_projection_figs/babar_95_comparison/babar95_vs_projected_full_data_100pct.png}
\caption{Projected full-data HPS simultaneous reach compared with the BaBar observed
visible-dark-photon exclusion converted to 95\% CL on $\eps^2$
\cite{BaBarDarkPhoton2014}. The lower panel shows projected HPS divided by BaBar.
Values below one indicate projected HPS reach beyond the BaBar curve. In this
projection HPS improves on BaBar in several low-mass pockets, but BaBar remains
stronger over much of the high-mass range.}
\label{fig:results-babar-projection}
\end{figure}

\subsection{90\% CLs comparison suite}
\label{sec:results-90cls-suite}

The 90\% CLs rerun provides a useful cross-check of the final-limit machinery and a
like-for-like comparison to external 90\% contours. The corrected combined result in
this subsection uses the \path{v2} combined-only CSV exports from
\path{obsUL90_combined_only_2015_2016_10pct_2021_1pct_blind2p25_lslb1p0_rpen7_dens1p64_10k_corrected}.
Where the \path{v2} folder does not contain the corresponding individual observed
band CSVs, the individual 90\% curves and bands are taken from the \path{v1}
single-dataset CSVs. This keeps the simultaneous result tied to the corrected
count-scale combined scan while preserving the individual inputs needed for the same
diagnostic comparisons shown above at 95\% CL.

\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/individual_datasets/2015_90cls_eps2_coupling_bands_observed_expected.png}
  \caption{2015.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/individual_datasets/2016_90cls_eps2_coupling_bands_observed_expected.png}
  \caption{2016 10\%.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.32\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/individual_datasets/2021_90cls_eps2_coupling_bands_observed_expected.png}
  \caption{2021 1\%.}
\end{subfigure}
\caption{Individual 90\% CLs upper limits on $\eps^2$ from the \texttt{v1}
single-dataset CSVs. These panels are used as diagnostic inputs to the 90\%
combined-versus-individual comparison; the simultaneous 90\% limit is still obtained
from the corrected shared-coupling combined scan.}
\label{fig:results-90cls-individuals}
\end{figure}

\begin{figure}[H]
\centering
\graphicorplaceholder{0.90\linewidth}{final_limit_projection_figs/90cls/combined/combined_90cls_bands_corrected_eps2.png}
\caption{Corrected combined 90\% CLs observed and expected upper limit on $\eps^2$.
The observed curve is taken from the corrected simultaneous scan, and the expected
bands are repaired only at the isolated coherent expected-band spikes identified in
Figure~\ref{fig:results-90cls-repair-diagnostics}.}
\label{fig:results-90cls-combined-band}
\end{figure}

The 90\% combined overlay and ratio diagnostics in
Figure~\ref{fig:results-90cls-overlay-ratio} follow the same convention as the 95\%
diagnostics. The combined curve is compared only with active individual datasets at
each mass hypothesis, and the ratio is formed in $\eps$ so that it directly compares
coupling reach rather than $\eps^2$ reach.

\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/combined/observed_90cls_eps2_overlay_individual_and_combined.png}
  \caption{Observed individual and combined 90\% CLs limits.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/diagnostics/combined_vs_best_individual_observed_epsilon_ratio_90cls.png}
  \caption{Combined over best active individual observed limit in $\eps$.}
\end{subfigure}
\caption{Observed 90\% CLs combination diagnostics. The overlay shows the individual
observed limits and the corrected simultaneous observed limit on $\eps^2$. The ratio
panel shows where the simultaneous fit improves on the best active individual observed
limit and where an individual observed fluctuation gives a stronger pointwise limit.}
\label{fig:results-90cls-overlay-ratio}
\end{figure}

\begin{figure}[H]
\centering
\graphicorplaceholder{0.90\linewidth}{final_limit_projection_figs/90cls/diagnostics/combined_90_vs_95cls_observed_expected_eps2.png}
\caption{Combined 90\% and 95\% observed and expected-median upper limits on
$\eps^2$. The lower panel shows the 95\%/90\% ratio and the one-sided Gaussian
confidence-level scaling for orientation. Deviations from a flat Gaussian ratio track
the mass-dependent CLs construction, interpolation, and observed-curve corrections.}
\label{fig:results-90cls-vs-95cls}
\end{figure}

\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/projections/projected_full_unblinded_reach_90cls_eps2.png}
  \caption{Projected full-unblinded 90\% reach.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/babar_90_comparison/babar90_vs_projected_full_data_100pct.png}
  \caption{Projected HPS reach compared with BaBar at 90\%.}
\end{subfigure}
\caption{Projected 90\% CLs reach comparisons. The full-unblinded projection scales
the current 2016 10\% and 2021 1\% density inputs to 100\% while holding the 2015
sample fixed. The BaBar comparison is projection-only on the HPS side: the current
observed 90\% combined curve is intentionally omitted, and the lower panel shows
projected HPS divided by the BaBar 90\% contour.}
\label{fig:results-90cls-projection-babar}
\end{figure}

\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/diagnostics/combined_expected_band_spike_diagnostics_90cls.png}
  \caption{Expected-band spike repairs.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\linewidth}
  \centering
  \graphicorplaceholder{\linewidth}{final_limit_projection_figs/90cls/diagnostics/combined_toy_limit_tail_areas_90cls.png}
  \caption{Toy-tail diagnostics.}
\end{subfigure}
\caption{90\% CLs diagnostic plots used to audit the two corrected expected-band
points and the behavior of the observed limit relative to the background-only toy
limit ensemble. The toy-tail panel is plotted as p-value versus mass on a logarithmic
y-axis and includes the local and Sidak scan-level global significance-reference
thresholds used in the corresponding 95\% CLs diagnostic. The expected-band repair is
limited to coherent one-point excursions at 103 and 198 MeV, where each expected-band
quantile is replaced by the log-linear
interpolation between neighboring mass hypotheses in the same active dataset set.}
\label{fig:results-90cls-repair-diagnostics}
\end{figure}

\subsection{Forward placeholders}
\label{sec:results-forward-placeholders}

\analysisplaceholder{\textbf{First-round unblinding.} Reserve this subsection for the
2015$+$2016 10\%$+$2021 10\% milestone once the corrected look-elsewhere rerun is
available.}

\analysisplaceholder{\textbf{Final combined analysis.} Reserve this subsection for the
fully unblinded 2015$+$2016$+$2021 prompt-resonance result. The completed version
should quote only the validated analysis range, the final scan-level global
significance calibration, and the final radiative-fraction inputs.}
